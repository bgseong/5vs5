node {
    def hello = 'Hello bgsung' // 변수선언
    stage ('clone') {
        git 'https://github.com/bgseong/5vs5.git' // git clone
    }
    
    stage ('print') {
        print(hello) // 함수 + 변수 사용
    }
}
